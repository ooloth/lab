const Ball = ({ className, style }) => (
  <aside
    role="presentation"
    aria-hidden="true"
    className={`ball ${className}`}
    style={style}
  />
)

/* 
 *
 * Imports & Exports
 * 
 */

import React from 'react'

export default Ball
